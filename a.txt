# python -m venv .venv
# .venv\Scripts\activate
# pip install django
# python.exe -m pip install --upgrade pip
# django-admin startproject civiclink
# python manage.py runserver
# python manage.py makemigrations
# python manage.py migrate
# manage.py createsuperuser 


# in setting.py add--- 
# MEDIA_URL = '/media/'
# MEDIA_ROOT = os.path.join(BASE_DIR, 'media')
# STATIC_URL = 'static/'
# STATICFILES_DIRS = [os.path.join(BASE_DIR, 'static')]

# in urls
# from django.conf import settings 
# from django.conf.urls.static import static


# urlpatterns = [
#     path('admin/', admin.site.urls),
# ] + static(settings.MEDIA_URL, document_root = settings.MEDIA_ROOT)


python manage.py startapp Civiclink 
url i main copy ,url in app paste 
views - render html
again fetch html in url

add app in main and add in DIRS=[os.path.join(BASE_DIR, 'templates')] in templates in settings.py


write html in app and include url in urls
im models.py write models,makemigration,migrate,register in admin
    from .models import Tweet
    # Register your models here.
    admin.site.register(Tweet)
create forms.py and import form amd models both
class TweetForm(forms.ModelForm):
    class Meta:
        model = Tweet
        field = ['text', 'photo']
in views.py create class to perform crud operation for form 
in templates of app,make necessary html and set href=url(the url is the class of form which redirects the html)
//<img src="{{tweet.photo.url}}" class="card-img-top" alt="..." />
again set url in urls.py using path
if in view it is written validation of form then in html it is compulsory to write
{% csrf_token %} for edit/delete etc...


in project's templates make necessary html

in app views.py ,make register method

in settings.py write login url and login rdirect ul and logout url
in urls.py add url using path and include django.contrib.auth.urls

